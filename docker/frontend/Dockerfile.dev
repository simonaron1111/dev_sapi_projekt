FROM node:24.7.0-alpine

WORKDIR /app

# Install dependencies will be done at runtime via volume mount
# This image just provides the Node.js environment

CMD ["sh", "-c", "npm install && npm start -- --host 0.0.0.0 --port 4200"]

